// import { Router } from 'express'

// import * as db from '../db/tlds.ts' //double check db naming

// const router = Router()

// router.get('/', async (req, res) => {
//   try {
//     const domains = await db.getTlds()

//     res.json({ domains: domains.map((tld) => tld.name) })
//   } catch (error) {
//     console.log(error)
//     res.status(500).json({ message: 'Something went wrong' })
//   }
// })

// export default router
